# v0.1.9 원정 프리셋

- 저장 schema를 v7로 올리고 최대 5개의 `expeditionPresets` 슬롯을 추가했습니다.
- v6 저장은 `tower-record-v1-before-presets-v7`에 한 번 백업한 뒤 기존 상태를 보존하고 빈 슬롯만 추가합니다.
- 일반 이용자는 1~2번을 사용합니다. 3~5번은 데이터가 있어도 삭제하지 않고 접근만 잠그며, 향후 slot limit을 5로 바꾸면 그대로 복구됩니다.
- 프리셋은 개별 장비 instance ID 4개, 자동 스킬 3개, 포션별 휴대 수량, HP 자동사용 기준을 snapshot으로 저장합니다.
- 불러오기 전에 장비 보유, 스킬 습득, 포션 수량을 모두 검사합니다. 오류가 하나라도 있으면 어떤 설정도 적용하지 않습니다.
- 프리셋 불러오기는 원정을 시작하거나 입장권·Silver·외형·칭호·탑·층을 변경하지 않습니다.
- 진행 중 원정에서는 저장, 덮어쓰기, 이름 변경과 불러오기를 모두 차단합니다.
