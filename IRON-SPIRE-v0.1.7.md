# v0.1.7 철맥의 첨탑 1~10층

- 저장 호환성을 위해 내부 탑 ID `ore`와 저장 schema v5를 유지하고 표시 이름만 `철맥의 첨탑`으로 바꿨습니다.
- 1~10층 일반 출현 풀에 고블린 광부, 동굴 쥐, 광산 박쥐, 고블린 운반꾼, 고블린 감독관을 구간별로 배치했습니다.
- 10층에는 `bossId: mining_ogre` metadata를 추가했습니다. 광산 오우거는 일반 출현 풀에 포함되지 않으며 보스 추적 기능은 구현하지 않았습니다.
- 기존 `ore` T1 재료 ID를 유지하면서 화면 이름을 `철광석`으로 정리했습니다.
- 몬스터 이미지는 `assets/monsters/iron-t1/<id>.png`, T1 배경은 `assets/backgrounds/iron-t1-abandoned-mine.png`를 사용합니다. 파일이 없으면 기존 이미지 fallback UI가 동작합니다.
- Silver, 재료, 입장권과 스킬북은 기존 원정 임시 전리품과 안전 귀환 규칙을 그대로 사용합니다.
