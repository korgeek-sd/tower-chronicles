import React from 'react';
import type {InventoryViewItem} from '../../game/inventoryView';
import {InventoryIcon} from './InventoryDetailSheet';
export function InventoryGrid({items,selected,onSelect}:{items:InventoryViewItem[];selected:string|null;onSelect:(key:string)=>void}){return items.length?<div className="inventory-grid">{items.map(i=><button key={i.key} className={'inventory-slot '+(selected===i.key?'selected':'')} aria-label={i.name+' · '+i.quantity+'개'+(i.equipped?' · 장착 중':'')} aria-pressed={selected===i.key} onClick={()=>onSelect(i.key)}><InventoryIcon id={i.iconId}/>{i.tier&&<span className="item-tier">T{i.tier}</span>}{i.stack&&<span className="item-count">{i.quantity.toLocaleString()}</span>}{!!i.enhancement&&<span className="item-enhancement">+{i.enhancement}</span>}{i.equipped&&<span className="item-equipped">장착</span>}</button>)}</div>:<p className="inventory-empty" role="status">조건에 맞는 아이템이 없습니다.</p>;}
