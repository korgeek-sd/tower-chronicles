# v0.1.10 — 황금기록자 권한 시스템

## 구현 내용

- 저장 스키마를 v8로 올리고 `goldenRecorder.expiresAt` 만료 시각만 영구 저장합니다.
- 현재 시각이 만료 시각보다 앞선 경우에만 활성으로 판정하며, 만료 정각에는 비활성화됩니다.
- 일반 계정은 원정 프리셋 2개, 황금기록자는 5개를 사용할 수 있습니다. 만료되어도 3~5번 슬롯 데이터는 삭제하지 않습니다.
- 황금기록자 활성 중에는 기존 제작 숙련도 할인에 2%를 더합니다. 기존 올림 계산과 최소 재료 1개 규칙은 유지합니다.
- 향후 거래소가 사용할 판매 수수료 값은 일반 2%, 황금기록자 1%로 중앙 혜택 도우미에서 제공합니다.
- 상단에 현재 상태를 표시하고 상세 화면에서 남은 기간, 만료 일시, 적용 혜택을 확인할 수 있습니다.
- 개발 서버에서만 기간 추가와 즉시 만료 QA 버튼을 표시합니다.

## 저장 이전

v7 저장은 최초 로드 시 원본을 `tower-record-v1-before-golden-recorder-v8` 키에 한 번 백업한 뒤 v8로 이전합니다. 재산, 원정, 프리셋, 장비, 외형, 숙련도와 기타 진행 상태는 그대로 보존됩니다. 유효하지 않은 만료 시각과 백업 실패는 메인 저장을 덮어쓰지 않습니다.

## 검증

황금기록자 판정, 만료 경계, 혜택, 프리셋 잠금과 복구, 제작 할인, 기간 표기와 연장, 저장 검증 및 v7 이전을 자동 테스트로 확인합니다.
