# 탑의 기록 v0.1.1 변경 보고

## 변경한 데이터 구조

- 저장 버전을 `2`로 올렸다.
- `Expedition.loot`에 원정 중 획득한 골드, 탑별·티어별 재료, 탑별·층별 입장권, 스킬북, 향후 기타 전리품을 분리해 보관한다.
- 영구 스킬북 인벤토리 `GameState.skillBooks`와 기타 전리품 `GameState.lootItems`를 추가했다.
- `GameState.lastExpedition`은 귀환·사망 결과 표시용 영수증이다. 보상 정산에는 다시 사용할 수 없다.

## 처리 방식

- 몬스터 처치 보상은 영구 골드·재료·티켓·스킬에 직접 반영하지 않고 `expedition.loot`에만 쌓인다.
- 안전 귀환은 활성 원정을 먼저 제거하고 전리품 전체와 남은 포션을 한 번에 영구 보관함에 합산한다. 다시 귀환을 호출해도 활성 원정이 없어 아무 보상도 지급되지 않는다.
- 사망은 활성 원정을 제거하고 전리품과 남은 원정 포션을 지급하지 않는다. 기존 영구 재산은 변경하지 않는다.
- 결과 화면에는 이번 원정의 보관 또는 손실 내역만 표시한다.
- 스킬북은 귀환 후 인벤토리에서 직접 사용해야 배운다. 이미 배운 스킬의 책도 일반 아이템으로 계속 보유할 수 있다.

## 기존 저장 데이터

- v1 저장은 같은 localStorage 키에서 v2로 자동 이전한다.
- 비원정 저장은 기존 재산과 진행도를 그대로 유지한다.
- v1 진행 원정은 당시 전투 보상이 이미 영구 반영된 구조였으므로 원정을 안전하게 종료하고 남은 포션만 반환한다. 보상은 재지급하지 않는다.
- 이전 전 원본은 `tower-record-v1-before-loot-v2` 키에 한 번 백업한다.
- 손상되었거나 검증에 실패한 저장은 덮어쓰지 않는다.

## 변경한 주요 파일

- `src/game/types.ts`: v2 저장, 원정 전리품 및 결과 타입
- `src/game/engine/loot.ts`: 빈 전리품, 합계, 표시, 영구 정산
- `src/game/engine/drops.ts`: 전투 보상의 임시 적재
- `src/game/engine/expedition.ts`: 원자적 귀환 정산과 사망 폐기
- `src/game/engine/skills.ts`: 스킬북 직접 사용
- `src/storage/repository.ts`: v2 검증과 v1 마이그레이션
- `src/components/ExpeditionLoot.tsx`: 전투 획득물 및 결과 카드
- `src/main.tsx`, `src/style.css`: 전투·인벤토리 UI 연결
- `tests/loot.test.ts`: 신규 규칙 및 마이그레이션 테스트

## 검증

- `npm run typecheck`: 성공
- `npm test`: 44개 성공, 0개 실패
- `npm run build`: 성공
- 브라우저에서 귀환 전 영구 골드·광석이 변하지 않고 임시 원정 전리품만 증가하는 것을 확인했다.
- 안전 귀환 시 임시 전리품이 영구 보관함에 합산되는 것을 확인했다.
- 사망 시 이번 원정 골드·광석·입장권이 손실 내역에 표시되고, 기존 영구 골드·광석은 유지되는 것을 확인했다.
